const logoutView = () => {
  $('#nav').html(`
    <nav class="navbar navbar-light bg-light">
      <span class="navbar-brand mb-0 h1">Pinterest 2.0</span>
    </nav>
    `);
};

export default { logoutView };
